#!/bin/sh
echo ""
echo "커밋 메시지 검사 시작"
npx --no-install commitlint --edit "$1" || {
  echo "커밋 메시지가 규칙에 맞지 않습니다."
  echo "=================================================="
  echo "📝 커밋 메시지 규칙:"
  echo "[개발자 이름], [브랜치명]: [구현내용] #[관련 이슈 번호]"
  echo "구현 내용이 비어 있으면 안 됨"
  echo "제목 길이는 최대 72자"
  echo "브랜치명(타입)은 소문자여야 함"
  echo "허용되는 브랜치명 목록 : ['feat', 'hotfix', 'chore']"
  echo ""
  echo "예시: byungchan, chore: 프로젝트 초기 환경 설정 및 테스트 #1"
  echo "=================================================="
  exit 1 # 비정상 종료 코드 반환
}
echo "커밋 메시지가 규칙에 맞습니다!"
